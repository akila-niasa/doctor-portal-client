import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen m-64'>
            <button className="btn loading">loading</button>
        </div>
    );
};

export default Loading;